---
phase: 01-page-implementation
plan: 01
type: execute
depends_on: []
files_modified: [src/app/page.tsx, src/app/about/page.tsx, src/app/services/page.tsx, src/app/contact/page.tsx, src/components/layout/Header.tsx, src/components/layout/Footer.tsx, src/components/layout/MobileNav.tsx]
domain: next-js
---

<objective>
Implement core pages (Home, About, Services, Contact) with responsive mobile design and hardcoded content. Establish layout foundations (Header, Footer, Navigation) that all pages will use. These core pages form the foundation for Phase 1.

Purpose: Core pages must be production-ready and deployable individually so they can be reviewed and polished before moving to feature pages.
Output: Working, responsive Home, About, Services, and Contact pages with proper navigation and layout.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@src/app/layout.tsx
@src/components/layout/Header.tsx
@src/components/layout/Footer.tsx
@src/components/layout/MobileNav.tsx
@src/features/home/HeroSection.tsx
@src/lib/utils.ts
@package.json

**Tech stack available:** Next.js 16, React 19, TypeScript 5, Tailwind CSS 4, lucide-react icons
**Established patterns:** App Router pages with Server Components, feature-based component organization, cn() utility for class merging, mobile-first responsive design
**Key constraint:** All pages must be mobile-first responsive with hardcoded content (no Strapi yet)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit existing pages and update Home page layout</name>
  <files>src/app/page.tsx, src/features/home/HeroSection.tsx</files>
  <action>
  Review current home page layout. Update src/app/page.tsx to ensure it's a clean Server Component that imports feature components (HeroSection, ProcessStepsSection, TrustSection, TestimonialsCarousel, CoverageMapSection, ServicesStackSection, DecisionSection). Each section should use hardcoded content with clear data structures for future Strapi migration. Ensure all sections are responsive and mobile-first. Do NOT fetch from Strapi in Phase 1 - hardcode all content. Use Tailwind CSS 4 classes for responsive design. Import from src/lib/utils for cn() utility.
  </action>
  <verify>
  npm run build succeeds, page renders in development with all sections visible, responsive on mobile (375px, 768px, 1024px viewports).
  </verify>
  <done>
  Home page displays all sections, responsive mobile layout, no TypeScript errors, hardcoded content only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement About page with responsive sections</name>
  <files>src/app/about/page.tsx, src/features/about/AboutPage.tsx</files>
  <action>
  Create src/app/about/page.tsx as a Server Component. Create src/features/about/AboutPage.tsx with hardcoded content sections: hero/intro section, company values section, history timeline, team section (if applicable). Structure content as objects/arrays for easy Strapi migration later. Use Tailwind responsive classes (md:, lg: prefixes). Ensure mobile-first design. Include proper semantic HTML (h1, h2, p tags). Do NOT use Strapi API calls.
  </action>
  <verify>
  npm run build succeeds, /about route loads, all sections render correctly, mobile viewport displays properly without horizontal scroll.
  </verify>
  <done>
  About page complete with 4+ hardcoded content sections, fully responsive, TypeScript types defined.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Services page with service listings</name>
  <files>src/app/services/page.tsx, src/features/services/ServicesPage.tsx</files>
  <action>
  Create src/app/services/page.tsx as Server Component. Create src/features/services/ServicesPage.tsx displaying service catalog with hardcoded service items. Each service should have: title, description, icon, features/benefits. Organize services in a grid layout (1 column mobile, 2+ on tablet/desktop). Use lucide-react icons for service icons. Structure data as array of objects for Strapi content model. Include hero section at top.
  </action>
  <verify>
  npm run build succeeds, /services route loads, grid displays 1 column on mobile/tablet, 2+ on desktop.
  </verify>
  <done>
  Services page displays 6+ hardcoded services with icons and descriptions, responsive grid layout, semantic HTML.
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement Contact page with form placeholder and info</name>
  <files>src/app/contact/page.tsx, src/features/contact/ContactPage.tsx</files>
  <action>
  Create src/app/contact/page.tsx as Server Component. Create src/features/contact/ContactPage.tsx with: hero section, contact information (email, phone, address), map placeholder using Leaflet (existing integration), form placeholder (no submission logic in Phase 1). Structure contact info as hardcoded objects. Include "Get in touch" section with multiple contact methods. Form should be non-functional placeholder for now (no backend logic).
  </action>
  <verify>
  npm run build succeeds, /contact route loads, all contact info displays, form renders without submission.
  </verify>
  <done>
  Contact page displays contact information, map placeholder, form UI, responsive on all viewports.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Core pages (Home, About, Services, Contact) with responsive mobile design</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit and test each page on mobile viewport (375px):
       - http://localhost:3000 (Home)
       - http://localhost:3000/about (About)
       - http://localhost:3000/services (Services)
       - http://localhost:3000/contact (Contact)
    3. Verify:
       - All pages load without errors
       - Content displays properly on mobile (no horizontal scroll)
       - Navigation between pages works
       - Images/icons load correctly
       - Tailwind styling applied
    4. Test on tablet (768px) and desktop (1024px) to confirm responsive design
  </how-to-verify>
  <resume-signal>Type "approved" if all pages look good, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors or warnings
- [ ] All 4 core pages render without errors in development
- [ ] Mobile viewport (375px) displays correctly on all pages
- [ ] Responsive design works on tablet (768px) and desktop (1024px)
- [ ] Navigation between pages functions properly
- [ ] No TypeScript errors (`npx tsc --noEmit` passes)
- [ ] Images and icons load correctly
- [ ] Content is hardcoded (no Strapi API calls)
</verification>

<success_criteria>

- All 4 core pages (Home, About, Services, Contact) fully implemented
- All pages responsive and mobile-first
- All content hardcoded with clear data structures for Strapi migration
- No external API calls (Strapi unused in Phase 1)
- Each page individually deployable
- All pages pass TypeScript type checking
- No console errors or warnings
- Foundation layout (Header, Footer, Navigation) properly integrated
  </success_criteria>

<output>
After completion, create `.planning/phases/01-page-implementation/01-01-SUMMARY.md`:

# Phase 1 Plan 1: Core Pages Implementation Summary

**Core pages (Home, About, Services, Contact) implemented with responsive mobile design**

## Accomplishments

- Home page with all sections (hero, process, trust, testimonials, coverage, services, decision)
- About page with company information and history sections
- Services page with service catalog and grid layout
- Contact page with information and contact form placeholder
- All pages fully responsive on mobile first
- Hardcoded content structured for Strapi migration

## Files Created/Modified

- `src/app/page.tsx` - Home page
- `src/app/about/page.tsx` - About page
- `src/app/services/page.tsx` - Services page
- `src/app/contact/page.tsx` - Contact page
- `src/features/about/AboutPage.tsx` - About page component
- `src/features/services/ServicesPage.tsx` - Services page component
- `src/features/contact/ContactPage.tsx` - Contact page component

## Decisions Made

- Hardcoded all content for Phase 1, structured as objects/arrays for Strapi migration
- Mobile-first responsive design using Tailwind CSS 4
- Server Components for all pages (Next.js App Router pattern)
- No CMS integration in Phase 1 (planned for Phase 2)

## Issues Encountered

None

## Next Phase

Ready for 01-02-PLAN.md (Feature pages: Booking, Careers, Network, Legal)
</output>
