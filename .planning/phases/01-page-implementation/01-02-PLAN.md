---
phase: 01-page-implementation
plan: 02
type: execute
depends_on: ["01-01"]
files_modified: [src/app/booking/page.tsx, src/app/booking/schedule/page.tsx, src/app/booking/assessment/page.tsx, src/app/carrieres/page.tsx, src/app/carrieres/job/page.tsx, src/app/carrieres/[jobId]/page.tsx, src/app/notre-reseau/page.tsx, src/app/engagement/page.tsx, src/app/mentions-legales/page.tsx, src/features/booking/BookingFlow.tsx, src/features/career/CareerPage.tsx, src/features/network/NetworkMapPage.tsx]
domain: next-js
---

<objective>
Implement feature pages (Booking flow, Careers, Network map, Engagement, Legal) with responsive mobile design and hardcoded content. These pages build on the core pages from Plan 01. Feature pages include multi-step flows and more complex layouts.

Purpose: Feature pages provide key user journeys (booking service, viewing jobs, network coverage). Must be mobile-responsive and ready for individual deployment.
Output: Working, responsive Booking, Careers (with job detail), Network, Engagement, and Legal pages with proper navigation and layout.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
@.planning/phases/01-page-implementation/01-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-page-implementation/01-01-SUMMARY.md
@src/app/layout.tsx
@src/features/booking/BookingFlow.tsx
@src/features/career/CareerPage.tsx
@src/features/home/MoroccoCentresMap.tsx
@src/lib/utils.ts
@src/lib/api/client.ts
@package.json

**Tech stack available:** Next.js 16, React 19, TypeScript 5, Tailwind CSS 4, lucide-react icons, Leaflet maps, qs for query strings
**Established patterns:** Server Components, feature-based organization, responsive Tailwind design, existing Leaflet integration, multi-page flows (booking already has schedule/assessment paths)
**Key constraint:** All pages must be mobile-first responsive with hardcoded content (no Strapi yet)
**Prior decisions:** Hardcode content in Phase 1 structured for Strapi, mobile-first responsive design
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Booking flow (main, schedule, assessment pages)</name>
  <files>src/app/booking/page.tsx, src/app/booking/schedule/page.tsx, src/app/booking/assessment/page.tsx, src/features/booking/BookingFlow.tsx</files>
  <action>
  Review existing booking structure. Update src/app/booking/page.tsx as entry point listing booking options/services with hardcoded data. Ensure src/app/booking/schedule/page.tsx and src/app/booking/assessment/page.tsx exist and display appropriate content for each step. Create src/features/booking/BookingFlow.tsx component managing the flow state/progression (if not exists). Each page shows next/prev buttons for navigation. Structure content as objects/arrays for Strapi migration. Use Tailwind responsive classes. Mobile-first design ensuring each step is clear and uncluttered on small screens.
  </action>
  <verify>
  npm run build succeeds, /booking and sub-routes load, can navigate between booking, schedule, assessment pages on mobile viewport.
  </verify>
  <done>
  Booking flow with 3 pages (main, schedule, assessment) responsive and interconnected, hardcoded content, TypeScript types.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Careers pages (listings, job detail)</name>
  <files>src/app/carrieres/page.tsx, src/app/carrieres/job/page.tsx, src/app/carrieres/[jobId]/page.tsx, src/features/career/CareerPage.tsx</files>
  <action>
  Create/update src/app/carrieres/page.tsx as careers main page listing all job openings with hardcoded data (title, location, type, salary range if applicable). Use grid/list layout responsive to mobile. Each job item should link to job detail page. Create src/app/carrieres/[jobId]/page.tsx for individual job detail page showing: title, description, requirements, responsibilities, benefits, apply link (form placeholder in Phase 2). Ensure navigation back to listings works. Structure job data as array of objects with proper types for Strapi migration.
  </action>
  <verify>
  npm run build succeeds, /carrieres lists jobs, clicking job goes to /carrieres/[jobId], back navigation works, mobile responsive (1 column).
  </verify>
  <done>
  Careers page lists 6+ hardcoded jobs, individual job detail pages work, responsive single-column mobile layout.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Network/Coverage page with Leaflet map</name>
  <files>src/app/notre-reseau/page.tsx, src/features/network/NetworkMapPage.tsx</files>
  <action>
  Create src/app/notre-reseau/page.tsx as network/coverage page. Create src/features/network/NetworkMapPage.tsx component that displays: interactive Leaflet map (reuse existing MoroccoCentresMap component if applicable) showing coverage/office locations with hardcoded location data (name, address, coordinates, contact info). Include list of locations below or beside map (toggleable on mobile). Each location has hardcoded info. On mobile, prioritize map full-width with location list below. Use responsive grid for location cards.
  </action>
  <verify>
  npm run build succeeds, /notre-reseau loads, map renders, locations display on mobile in responsive layout.
  </verify>
  <done>
  Network page displays interactive map with 5+ hardcoded locations, location list, responsive on mobile (map full-width, list below).
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement Engagement and Legal pages</name>
  <files>src/app/engagement/page.tsx, src/app/mentions-legales/page.tsx, src/features/engagement/EngagementPage.tsx</files>
  <action>
  Create src/app/engagement/page.tsx displaying engagement/commitments section with hardcoded content (values, statements, commitments). Create src/app/mentions-legales/page.tsx for legal/terms page with hardcoded legal text sections (terms, privacy statement, etc.). Both pages should have hero/intro section, followed by content sections. Use Tailwind for responsive typography ensuring readability on mobile. Structure legal content as paragraphs/sections for easy future management in Strapi.
  </action>
  <verify>
  npm run build succeeds, /engagement and /mentions-legales load, content displays properly on mobile without text overflow or readability issues.
  </verify>
  <done>
  Engagement and Legal pages fully implemented with hardcoded content, responsive typography, mobile-readable.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Feature pages (Booking, Careers, Network, Engagement, Legal) with responsive mobile design</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit and test on mobile viewport (375px):
       - http://localhost:3000/booking (then navigate through booking flow)
       - http://localhost:3000/carrieres (view jobs list)
       - Pick a job and view details (e.g., first job)
       - http://localhost:3000/notre-reseau (view network map)
       - http://localhost:3000/engagement (view engagement info)
       - http://localhost:3000/mentions-legales (view legal)
    3. Verify:
       - All pages load without errors
       - Mobile layout is clean (1 column, no horizontal scroll)
       - Navigation between related pages works (e.g., job list → detail → back)
       - Map renders on network page
       - Text is readable and properly formatted
       - All interactive elements (links, buttons) work
    4. Test on tablet/desktop viewports for responsive behavior
  </how-to-verify>
  <resume-signal>Type "approved" if all feature pages look good, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors or warnings
- [ ] All 5 feature pages render without errors in development
- [ ] Booking flow navigation works (main → schedule → assessment)
- [ ] Careers listings and job detail pages functional
- [ ] Network map renders with locations
- [ ] Mobile viewport (375px) displays correctly on all pages
- [ ] Responsive design works on tablet (768px) and desktop (1024px)
- [ ] No TypeScript errors (`npx tsc --noEmit` passes)
- [ ] All pages load with hardcoded content (no Strapi API calls)
</verification>

<success_criteria>

- All 5 feature pages (Booking, Careers, Network, Engagement, Legal) fully implemented
- Booking multi-page flow working with proper navigation
- Careers with job listings and individual job detail pages
- Network page with Leaflet map and location data
- All pages responsive and mobile-first
- All content hardcoded with clear data structures for Strapi migration
- No external API calls (Strapi unused in Phase 1)
- Each page individually deployable
- All pages pass TypeScript type checking
- No console errors or warnings
  </success_criteria>

<output>
After completion, create `.planning/phases/01-page-implementation/01-02-SUMMARY.md`:

# Phase 1 Plan 2: Feature Pages Implementation Summary

**Feature pages (Booking, Careers, Network, Engagement, Legal) implemented with responsive mobile design**

## Accomplishments

- Booking flow with 3 pages (main, schedule, assessment) fully implemented
- Careers listing page with 6+ job openings
- Individual job detail pages with full information
- Network/coverage page with interactive Leaflet map and location listings
- Engagement page with hardcoded company commitments
- Legal/mentions page with terms and conditions
- All pages fully responsive on mobile first

## Files Created/Modified

- `src/app/booking/page.tsx` - Booking entry point
- `src/app/booking/schedule/page.tsx` - Booking schedule step
- `src/app/booking/assessment/page.tsx` - Booking assessment step
- `src/app/carrieres/page.tsx` - Careers listings
- `src/app/carrieres/[jobId]/page.tsx` - Job detail page
- `src/features/booking/BookingFlow.tsx` - Booking flow component
- `src/features/career/CareerPage.tsx` - Career page component
- `src/app/notre-reseau/page.tsx` - Network page
- `src/features/network/NetworkMapPage.tsx` - Network map component
- `src/app/engagement/page.tsx` - Engagement page
- `src/app/mentions-legales/page.tsx` - Legal page

## Decisions Made

- Booking flow uses hardcoded options, structured for Strapi
- Careers data structured as array of job objects
- Network map uses hardcoded location data with Leaflet
- All content hardcoded for Phase 1, ready for CMS migration in Phase 2
- Mobile-first responsive design for all pages

## Issues Encountered

None

## Next Phase

Phase 1 complete. Ready for Phase 2: Progressive Deployment (set up deployment infrastructure and deploy pages individually)
</output>
